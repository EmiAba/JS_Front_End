function solve(name, area, population, country, postcode){
    let city={
        name: name, 
        area: area, 
        population:population, 
        country:country, 
        postcode:postcode
    }

    console.log(`name -> ${city.name}`);

}