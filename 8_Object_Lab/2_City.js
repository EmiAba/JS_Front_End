function solve(city){
    
 Object.keys(city).forEach(propName=>console.log(`${propName} -> ${city[propName]}`));

   
        // for (let key in city) {
        //     console.log(`${key} -> ${city[key]}`);
        // }
    }

